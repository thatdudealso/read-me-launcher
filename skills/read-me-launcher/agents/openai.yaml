interface:
  display_name: "Read Me Launcher"
  short_description: "Flashy PNG README heroes that actually render on GitHub"
  default_prompt: "Launch a museum-quality README with PNG heroes that render on GitHub camo. Avoid CSS-class SVGs as primary images. Keep code fences outside HTML divs. Run check-readme.sh. No secrets."
