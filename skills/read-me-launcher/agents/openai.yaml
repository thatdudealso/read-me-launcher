interface:
  display_name: "Read Me Launcher"
  short_description: "Consumer-first READMEs: launch flash or library code-proof"
  default_prompt: "Launch a consumer-grade README. Pick audience mode (launch/library/internal). For shareable products use PNG proof-heroes (banner+preview+showcase). For libraries lead with working code. Keep fences outside HTML divs. Run check-readme.sh. No secrets."
